import { type ReactNode } from "react";
import type {
    AppMailProps,
    AppMailReplyProps,
    AppMailSidebarItem,
    ChartDataState,
    ChartOptionsState,
    CustomEvent,
    Demo,
    LayoutType,
    MailKeys,
    SortOrderType,
} from "./demo";
import {
    type AppBreadcrumbProps,
    type AppConfigProps,
    type AppMenuItemProps,
    type AppTopbarRef,
    type Breadcrumb,
    type BreadcrumbItem,
    type ChatContextProps,
    type ColorScheme,
    type LayoutConfig,
    type LayoutContextProps,
    type LayoutState,
    type MailContextProps,
    type MenuContextProps,
    type MenuModel,
    type MenuProps,
    type NodeRef,
    type Page,
    type TaskContextProps,
    type UseSubmenuOverlayPositionProps,
} from "./layout";

type ChildContainerProps = {
    children: ReactNode;
};

export type {
    AppBreadcrumbProps, AppConfigProps, AppMailProps, AppMailReplyProps, AppMailSidebarItem, AppMenuItemProps, AppTopbarRef, Breadcrumb, Breadcrumb, BreadcrumbItem, ChartDataState,
    ChartOptionsState, ChatContextProps, ChildContainerProps, ColorScheme, CustomEvent, Demo, LayoutConfig, LayoutContextProps, LayoutState, LayoutType, MailContextProps, MailKeys, MenuContextProps, MenuModel, MenuProps, NodeRef, Page, SortOrderType, TaskContextProps, UseSubmenuOverlayPositionProps
};

